import {HomePage, SignIn} from "@pages";

const publicPaths = [
    {
        path: "/",
        component: HomePage
    },
    {
        path: "/auth",
        component: SignIn
    }
]

export default publicPaths